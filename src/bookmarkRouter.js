const express = require('express');
const bookmarkRouter = express.Router();
const logger = require('./logger');


bookmarkRouter 
    .route('/')
    .get((req, res) => {

    })
    .post((req, res) => {

    });